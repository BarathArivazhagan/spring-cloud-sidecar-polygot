server:
  port: 9500

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BarathArivazhagan/spring-cloud-sidecar-sample
          search-paths:
            - config-properties/{application}
          default-label: develop
  security:
    user:
      name: configuser
      password: configpassword
      