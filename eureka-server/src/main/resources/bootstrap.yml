   
spring:
  application:
    name: eureka-server
  profiles:
    active:
      - local
  cloud:
    config:
      uri:
        - http://localhost:9500
      username: configuser
      password: configpassword
     